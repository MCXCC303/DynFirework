# 元素列表

元素列表位于主窗口左侧，以树形结构展示项目中的所有元素。

## 结构

列表按元素类型分为两个分组。括号中的数字表示该分组下的元素数量。点击分组名可折叠或展开。

## 操作

用鼠标左键单击元素来选中元素。

在条目上点击右键弹出上下文菜单：

- **新建轨迹**：创建一个新的轨迹元素。
- **新建烟花**：创建一个新的烟花元素。
- **新建轨迹烟花**：创建一个组合的轨迹烟花元素。
- **复制元素**：复制当前选中的元素。
- **删除元素**：删除当前选中的元素。

在分组名上右键可使用「新建轨迹」和「新建烟花」。
