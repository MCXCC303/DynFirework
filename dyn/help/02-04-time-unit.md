# 时间单位换算

DynFirework 使用 Minecraft tick 作为基本时间单位。

## tick 与秒

1 tick = 1/20 秒（或 1 秒 = 20 tick）

## tick 与拍

1 拍对应的 tick 数由项目 BPM 决定。默认 BPM 为 120，此时 1 拍 = 0.5 秒 = 10 tick。

即每拍 tick 数 = 1200 / BPM。

## BPM 设置

通过项目设置更改项目的 BPM。
